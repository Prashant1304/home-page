const NestedLayout = ({children}:
  {children: React.ReactNode}
    ) => {
    return(
        <div>
            this is nested layout
        {children}
        </div>
    )
}

export default NestedLayout
